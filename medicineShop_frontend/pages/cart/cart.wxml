<!--<text wx:for="{{cartData}}" wx:key="index">-->
<!--   {{index}}:{{item}}-->
<!--</text>-->
<buyList cartDetail="{{cartDetail}}"></buyList>

<van-submit-bar
        price="{{ 3050 }}"
        button-text="提交订单"
        bind:submit="onSubmit"
/>
